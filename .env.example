# Copy this file to .env and fill in your real values.
# .env is gitignored — never commit real keys.

# OpsFlood FastAPI backend URL (no trailing slash)
BASE_URL=https://android-flood-app-production.up.railway.app

# Optional: override API timeout in seconds (default 10)
# API_TIMEOUT=10
