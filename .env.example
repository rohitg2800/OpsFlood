# Equinox Flood — Environment Variables
# Copy this file to .env and fill in your values.
# .env is gitignored and must NEVER be committed.

# Primary backend URL (Render deployment)
BASE_URL=https://opsflood.onrender.com

# Secondary/backup URL (leave empty if not available)
BACKUP_URL=
