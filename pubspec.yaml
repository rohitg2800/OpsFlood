name: floodops_app
description: OpsFlood - AI-powered flood prediction app
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.0
  fl_chart: ^1.2.0
  shared_preferences: ^2.2.2
  intl: ^0.20.2
  url_launcher: ^6.2.5
  # lottie removed — no .json assets exist in the project
  flutter_local_notifications: ^21.0.0
  connectivity_plus: ^7.1.1
  cupertino_icons: ^1.0.6

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
